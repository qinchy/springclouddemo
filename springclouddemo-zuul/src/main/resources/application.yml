eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8081/eureka/
spring:
  application:
    name: gateway
server:
  port: 8084
  context-path: /zuul
zuul:
  routes:
    api1: /zuul/api1/**
    api2: /zuul/api2/**